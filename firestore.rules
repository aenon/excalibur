rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Helper: is user authenticated?
    function isAuth() {
      return request.auth != null;
    }

    // Deny all by default — rules added per feature
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
